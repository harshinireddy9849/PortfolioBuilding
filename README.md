# Activity 1: C/C++ Environment Setup

## Student Information
- **Name:** Harshini reddy
- **Section / ID:** 3A / R25EF205

## Overview
This document details the setup of the C/C++ development environment in Visual Studio Code on macOS.

## Tools & Extensions Installed
- **VS Code Extensions:**
  - C/C++ (Microsoft)
  - C/C++ Extension Pack
- **Compiler:** Clang / Apple Command Line Tools (GCC alias)

## Verification Steps Completed
1. **Source Code:** Created `hello.c` containing a basic standard output program printing `"hello, world"`.
2. **Compilation:** Executed `gcc hello.c -o hello` via the VS Code integrated terminal.
3. **Execution:** Ran `./hello` to verify successful console output.
4. **Compiler Version:** Verified terminal path configuration using `gcc --version` and `g++ --version`.

## Notes & Troubleshooting
- Installed Xcode Command Line Tools (`xcode-select --install`) to enable standard C/C++ build tools.
- Cleared previous terminal output to ensure clean execution log.

## Collaboration Log

- Pairing partner: Chethana poorvi
- GitHub username: @Euna308
- What we built: Added a greet() function to greet a person by name.
- What I learned: I learned how GitLens can show who changed each line and how Live Share allows two people to code together in real time.